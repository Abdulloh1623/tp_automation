// `(.)mijozlar/[id]` bilan bir xil muammo — "dublikatlar" ham mijoz ID
// sifatida ilinib qolib, soft navigatsiyada 404 chiqarardi. Izoh uchun
// `../yangi/page.tsx` ga qarang.
export default function ModalDefaultForDuplicates() {
  return null;
}
